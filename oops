we want to create a new dialog, modify variant, with a shortcut / command `control-x v`. it should open a dialog with the current variant shown, starting with the variant name. if there is no variant, open the current provider + model with an empty variant name, and the current settings or defaults. for now support only openai and anthropic model variation config ("reasoningEffort" / "budgetTokens"), but we need a pluggable tool to generate different form controls for the different variant settings that models have. we need an cancel and ok button on the bottom right, and a delete button on the bottom left. this is going to be editing the user's opencode config file, changing it's json.
